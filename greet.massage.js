function greetMassage(firstName, language) {

  if (language === 'isixhosa') {
    return "Molo, " + firstName;
  }
  else if (language === 'english') {
    return "Hello, " + firstName;
  }
  else if (language === 'sesotho') {
    return "Dumela, " + firstName;
  }
}
